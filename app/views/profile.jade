extends layout

block head
  script(src="/javascripts/profile.js")

block navbar
  li
    a#update(href="#") Update Profile

block content
  .padding-all
    .header
      h1
        img(src="#{imageUrl}", alt=" ")
        | #{profileName}
    .design-w3l
      .mail-form-agile
        form
          input.padding(type="text", name="steamID64" placeholder="#{steamID64}")
          br
          input#submit(type="submit", value="Save profile")
          
